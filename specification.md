# 11v11 サッカー自動試合シミュレーション — 技術仕様書

## 1. プロジェクト概要

### 1.1 目的

本プロジェクトは、ブラウザ上で動作する **非インタラクティブ（自動再生）の11対11サッカー試合モックアップ** を提供することを目的とする。ユーザーの操作を一切必要とせず、キックオフからフルタイムまで自律的にシミュレーションが進行し、試合終了後は自動的に再スタートする。ESPN/DAZN風のスポーツ中継を模したビジュアルデザインを採用し、iPhone等のモバイル端末でも快適に閲覧できるレスポンシブ設計となっている。

### 1.2 技術スタック

本シミュレーションは以下の技術基盤の上に構築されている。

| 項目 | 技術 |
|------|------|
| フレームワーク | React 19 + TypeScript |
| ビルドツール | Vite 7 |
| 描画エンジン | HTML5 Canvas 2D Context |
| スタイリング | Tailwind CSS 4（Canvas外のリセットのみ） |
| フォント | Roboto Condensed（HUD見出し）、Roboto Mono（タイマー） |
| ループ制御 | `requestAnimationFrame` による可変フレームレート |

全シミュレーションロジック、AI、描画処理は単一ファイル `client/src/pages/Home.tsx`（約820行）に集約されており、外部ライブラリへの依存は存在しない。

### 1.3 ファイル構成

```
futsal-sim/
├── client/
│   ├── index.html              ← エントリHTML（Google Fonts読み込み）
│   └── src/
│       ├── main.tsx             ← Reactマウントポイント
│       ├── App.tsx              ← ルーティング・テーマ設定
│       ├── index.css            ← グローバルCSS（body margin/padding リセット）
│       └── pages/
│           └── Home.tsx         ← ★ 全シミュレーションコード（本仕様の対象）
├── server/
│   └── index.ts                ← 静的ファイル配信（本番用）
└── specification.md            ← 本仕様書
```

---

## 2. 座標系とピッチ寸法

### 2.1 座標系の定義

シミュレーションはワールド座標系（浮動小数点）で計算され、描画時にCanvas画素座標へ変換される。ワールド座標系の原点 `(0, 0)` はピッチの中心に位置し、X軸は水平方向（右が正）、Y軸は垂直方向（上が正）である。

> **注意**: Canvas描画時にはY軸が反転する（画面上方向がCanvas座標では小さい値）。変換関数 `w2s` がこの反転を処理する。

### 2.2 ピッチ寸法

FIFA規格の105m × 68mピッチを、1ユニット ≈ 5mのスケールで縮小している。以下の表にすべてのピッチ関連パラメータを示す。

| パラメータ | 値 | 実寸換算 | 説明 |
|-----------|-----|---------|------|
| `pitchHalfW` | 10.5 | 52.5m | ピッチ半幅（中心から左右端まで） |
| `pitchHalfH` | 6.8 | 34.0m | ピッチ半高（中心から上下端まで） |
| `goalHalfH` | 1.22 | 3.66m | ゴール半高（中心からポストまで） |
| `goalDepth` | 0.4 | 2.0m | ゴールネットの奥行き |
| `penAreaW` | 2.75 | 16.5m | ペナルティエリアの幅（ゴールラインから） |
| `penAreaH` | 3.35 | 20.15m | ペナルティエリアの半高 |
| `goalAreaW` | 0.92 | 5.5m | ゴールエリアの幅 |
| `goalAreaH` | 1.55 | 9.15m | ゴールエリアの半高 |
| `centreCircleR` | 1.53 | 9.15m | センターサークル半径 |
| `penSpotDist` | 1.83 | 11.0m | ペナルティスポット距離（ゴールラインから） |
| `cornerArcR` | 0.17 | 1.0m | コーナーアーク半径 |

ピッチ全体の有効範囲は `x ∈ [-10.5, 10.5]`、`y ∈ [-6.8, 6.8]` であり、すべてのプレイヤーとボールはこの範囲内にクランプされる。

### 2.3 チーム方向

| チーム | `team` 値 | 攻撃方向 | ゴール位置（X座標） |
|--------|----------|---------|-------------------|
| BLUE（左チーム） | -1 | 右方向（+X） | 守備: x = -10.5、攻撃目標: x = +10.5 |
| RED（右チーム） | +1 | 左方向（-X） | 守備: x = +10.5、攻撃目標: x = -10.5 |

---

## 3. データモデル

### 3.1 ベクトル型 `V`

すべての位置・速度・方向は2次元ベクトル `V = { x: number, y: number }` で表現される。以下のユーティリティ関数群が提供されている。

| 関数 | シグネチャ | 説明 |
|------|----------|------|
| `v` | `(x, y) → V` | ベクトル生成 |
| `vadd` | `(a, b) → V` | 加算 |
| `vsub` | `(a, b) → V` | 減算 |
| `vscl` | `(a, s) → V` | スカラー倍 |
| `vlen` | `(a) → number` | 長さ |
| `vnorm` | `(a) → V` | 正規化（ゼロベクトルの場合は `(1, 0)` を返す） |
| `vdist` | `(a, b) → number` | 2点間距離 |
| `vdot` | `(a, b) → number` | 内積 |
| `vlerp` | `(a, b, t) → V` | 線形補間 |
| `vang` | `(a, b) → number` | 2ベクトル間の角度（度数法） |
| `vmove` | `(from, to, d) → V` | `from` から `to` へ最大距離 `d` だけ移動 |
| `clamp` | `(v, lo, hi) → number` | 値の範囲制限 |
| `rng` | `(a, b) → number` | `[a, b)` の一様乱数 |
| `pitchClamp` | `(p) → V` | ピッチ範囲内にクランプ |

### 3.2 Player インターフェース

各プレイヤーは以下のプロパティを持つ。配列 `State.pl` のインデックス 0〜10 が BLUE チーム、11〜21 が RED チームに対応する。

| プロパティ | 型 | 説明 |
|-----------|---|------|
| `pos` | `V` | 現在位置（ワールド座標） |
| `team` | `number` | 所属チーム（-1: BLUE、+1: RED） |
| `num` | `number` | 背番号（1〜11） |
| `home` | `V` | フォーメーション上のホームポジション |
| `face` | `V` | 現在の向き（正規化ベクトル） |
| `act` | `"idle" \| "dribble" \| "move"` | 現在のアクション状態 |
| `tgt` | `V` | 移動目標位置 |
| `dt` | `number` | 次の意思決定までの残り時間（秒） |
| `isGK` | `boolean` | ゴールキーパーフラグ |
| `slot` | `number` | フォーメーション内のスロット番号（0〜10） |
| `role` | `Role` | 役割（`"GK"`, `"DEF"`, `"MID"`, `"FWD"`） |

### 3.3 Ball インターフェース

| プロパティ | 型 | 説明 |
|-----------|---|------|
| `pos` | `V` | 現在位置 |
| `vel` | `V` | 速度ベクトル（フリーボール時のみ有効） |
| `owner` | `number \| null` | 保持者のプレイヤーインデックス（`null` = 誰も保持していない） |
| `free` | `boolean` | フリーボール状態（パス・シュート後の飛行中、またはルーズボール） |
| `shot` | `boolean` | シュートフラグ（トレイル描画の区別に使用） |
| `dead` | `number` | ボールが低速で停止している累積時間（秒） |
| `cooldown` | `number` | 所有権変更後のインターセプト禁止時間（秒） |

### 3.4 State インターフェース

試合全体の状態を管理するトップレベルオブジェクトである。

| プロパティ | 型 | 説明 |
|-----------|---|------|
| `pl` | `Player[]` | 全22人のプレイヤー配列 |
| `ball` | `Ball` | ボール状態 |
| `sL` | `number` | BLUE チームの得点 |
| `sR` | `number` | RED チームの得点 |
| `time` | `number` | 経過時間（秒） |
| `over` | `boolean` | 試合終了フラグ |
| `paused` | `boolean` | 一時停止フラグ（ゴール後のリセット待ち） |
| `pauseT` | `number` | 一時停止の残り時間（秒） |
| `koSide` | `number` | 次のキックオフを行うチーム（-1 or +1） |
| `trail` | `Trail \| null` | パス/シュートの軌跡描画情報 |
| `flash` | `number` | 画面フラッシュの残り時間（秒） |
| `flashTxt` | `string` | フラッシュ時の表示テキスト（`"GOAL!"` / `"FULL TIME"`） |
| `restartT` | `number` | 試合終了後の自動リスタートまでの残り時間（秒） |

### 3.5 Trail インターフェース

パスやシュートの軌跡を一時的に画面上に表示するためのデータ構造である。

| プロパティ | 型 | 説明 |
|-----------|---|------|
| `start` | `V` | 軌跡の始点 |
| `end` | `V` | 軌跡の終点 |
| `shot` | `boolean` | シュートの軌跡か否か |
| `t` | `number` | 残り表示時間（秒、初期値 = `trailDuration`） |

---

## 4. フォーメーションとスロットシステム

### 4.1 4-4-2 フォーメーション

両チームとも **4-4-2** フォーメーションを採用している。以下の表は BLUE チーム（`team = -1`、左側配置）のホームポジションを示す。RED チームはこれをX軸・Y軸ともに反転（`x = -x, y = -y`）した位置に配置される。

| スロット | 背番号 | ポジション名 | 役割 | ホームポジション (x, y) |
|---------|--------|------------|------|----------------------|
| 0 | 1 | GK（ゴールキーパー） | GK | (-9.8, 0) |
| 1 | 2 | LB（左サイドバック） | DEF | (-7.5, -4.5) |
| 2 | 3 | CB（左センターバック） | DEF | (-7.5, -1.5) |
| 3 | 4 | CB（右センターバック） | DEF | (-7.5, 1.5) |
| 4 | 5 | RB（右サイドバック） | DEF | (-7.5, 4.5) |
| 5 | 6 | LM（左ミッドフィルダー） | MID | (-4.5, -5.0) |
| 6 | 7 | CM（左センターMF） | MID | (-4.5, -1.5) |
| 7 | 8 | CM（右センターMF） | MID | (-4.5, 1.5) |
| 8 | 9 | RM（右ミッドフィルダー） | MID | (-4.5, 5.0) |
| 9 | 10 | ST（左ストライカー） | FWD | (-1.5, -1.8) |
| 10 | 11 | ST（右ストライカー） | FWD | (-1.5, 1.8) |

### 4.2 スロットから役割への変換

`slotRole` 関数により、スロット番号から役割が一意に決定される。

```
slot 0       → "GK"
slot 1〜4    → "DEF"
slot 5〜8    → "MID"
slot 9〜10   → "FWD"
```

この役割はオフザボール時のAI行動（攻撃ラン、守備ポジショニング）を決定する際に使用される。

---

## 5. チューナブルパラメータ一覧

すべてのパラメータはソースコード冒頭の定数オブジェクト `P` に集約されており、値を変更するだけでシミュレーションの挙動を調整できる。

### 5.1 試合制御パラメータ

| パラメータ | 型 | デフォルト値 | 単位 | 説明 |
|-----------|---|------------|------|------|
| `matchDuration` | number | 120 | 秒 | 試合の総時間。カウントダウン表示される。 |
| `goalResetDelay` | number | 2.0 | 秒 | ゴール後、キックオフまでの待機時間 |

### 5.2 プレイヤー動作パラメータ

| パラメータ | 型 | デフォルト値 | 単位 | 説明 |
|-----------|---|------------|------|------|
| `moveSpeed` | number | 4.8 | units/s | ボール非保持時の移動速度 |
| `dribbleSpeed` | number | 3.8 | units/s | ドリブル時の移動速度 |
| `passSpeed` | number | 12 | units/s | パスのボール速度 |
| `shotSpeed` | number | 18 | units/s | シュートのボール速度 |
| `passAccuracy` | number | 0.80 | 0〜1 | パス精度。1.0で完全精度、低いほど誤差が大きい |
| `shotAccuracy` | number | 0.60 | 0〜1 | シュート精度。同上 |
| `dribbleControl` | number | 0.90 | 0〜1 | ドリブル成功率。失敗時はランダム方向にボールをロスト |
| `interceptRadius` | number | 0.75 | units | フリーボールのインターセプト判定半径 |
| `decisionInterval` | number | 0.20 | 秒 | AI意思決定の実行間隔 |
| `shotRange` | number | 5.5 | units | シュート可能な最大距離（ゴールまで） |
| `shotAngle` | number | 55 | 度 | シュート可能な最大角度（向きとゴール方向の差） |

### 5.3 ボール物理パラメータ

| パラメータ | 型 | デフォルト値 | 単位 | 説明 |
|-----------|---|------------|------|------|
| `looseBallDrag` | number | 3.5 | units/s² | フリーボールの減速率（摩擦） |
| `deadBallTime` | number | 0.7 | 秒 | ボールが低速で停止した場合の自動回収までの時間 |

### 5.4 描画パラメータ

| パラメータ | 型 | デフォルト値 | 単位 | 説明 |
|-----------|---|------------|------|------|
| `trailDuration` | number | 0.35 | 秒 | パス/シュート軌跡の表示時間 |
| `playerRadius` | number | 0.30 | units | プレイヤー円の半径 |
| `ballRadius` | number | 0.13 | units | ボール円の半径 |

---

## 6. AI意思決定システム

### 6.1 概要

各プレイヤーは `decisionInterval`（0.20秒）ごとに意思決定を行う。意思決定タイマー `dt` はフレームごとに減算され、0以下になると意思決定関数が呼び出される。ボール保持者と非保持者で異なるロジックが適用される。

```
毎フレーム:
  player.dt -= deltaTime
  if player.dt <= 0:
    player.dt = decisionInterval
    if ボール保持者:
      decideHasBall(state, playerIndex)
    else:
      decideNoBall(state, playerIndex)
```

### 6.2 ボール保持時の意思決定 (`decideHasBall`)

ボール保持者は以下の優先順位で行動を決定する。

#### 6.2.1 GK・自陣深い位置の特別処理

ゴールキーパー、または自陣の守備的3分の1（`team * pos.x > pitchHalfW * 0.33`）にいるプレイヤーは、常にパスを最優先する。パス先が見つからない場合は、前方へのセーフティクリアランス（`passSpeed * 0.8` の速度で前方8ユニットへ蹴り出す）を行う。この処理は、GKがボールを持ったまま動かなくなるデッドロックを防止するために導入された。

#### 6.2.2 通常の3段階優先ロジック

GK・自陣深い位置以外のプレイヤーは、以下の優先順位で判断する。

**優先度1: シュート (SHOT)**

条件: ゴールまでの距離が `shotRange`（5.5）以内、かつ現在の向きとゴール方向の角度差が `shotAngle`（55度）以内。

実行: ゴール中心に向かって `shotSpeed`（18 units/s）でボールを蹴る。`shotAccuracy` に基づく誤差（`(1 - shotAccuracy) * 3.0` の範囲でY座標にランダムオフセット）が加わる。シュートの軌跡は太いオレンジ色の線で `trailDuration` 秒間表示される。

**優先度2: パス (PASS)**

条件: シュート条件を満たさない場合、`bestPass` 関数で最適なパス先を探索する。

パス先評価スコアの計算式:

```
score = openness * 2 + goalProgress * 0.5 - distance * 0.12
if laneBlocked: score -= 4（通常モード）/ score -= 1.5（relaxedモード）
```

ここで `openness` は最も近い敵との距離、`goalProgress` は相手ゴール方向への進行度（`-team * pos.x`）、`distance` はパサーとの距離である。パス距離は 1.0〜18.0 の範囲に制限される。パスの軌跡は白い点線で `trailDuration` 秒間表示される。

**優先度3: ドリブル (DRIBBLE)**

条件: シュートもパスも不可能な場合のフォールバック。

実行: `dribbleControl`（0.90）の確率で成功する。成功時は、ゴール方向（重み0.55）、最寄りの敵からの回避方向（重み0.3）、壁回避ベクトル、ランダムなジンク（横方向の揺れ）を合成した方向へ4.0ユニット先を目標に移動する。失敗時はランダム方向にボールをロストする。

### 6.3 ボール非保持時の意思決定 (`decideNoBall`)

#### 6.3.1 GKの行動

ゴールキーパーは常にゴールライン付近（`team * (pitchHalfW - 0.6)`）に位置し、ボールのY座標に追従する（追従率50%、ゴールポスト内に制限）。ボールがフリーで2.5ユニット以内にある場合は、ボールに向かって飛び出す。

#### 6.3.2 ルーズボール追跡

ボールがフリー状態の場合、各チームから最も近い2人のフィールドプレイヤー（GKを除く）がボールを追跡する。追跡の判定は、チーム内での距離ランクが2位以内かつボールとの距離が12ユニット以内であることを条件とする。

#### 6.3.3 味方がボール保持時（攻撃ラン）

役割に応じて異なるポジショニングを行う。

| 役割 | 攻撃時の行動 |
|------|------------|
| DEF | ボール保持者の後方に位置し、ホームポジションのY座標を基準にボール位置に応じて微調整。ピッチの中央付近までは押し上げるが、それ以上は前進しない。 |
| MID（ワイド: slot 5, 8） | 保持者と相手ゴールの中間地点の35%付近に位置し、サイドに張る。 |
| MID（セントラル: slot 6, 7） | 保持者と相手ゴールの中間地点の40%付近に位置し、ボール位置からY方向に1.5〜3.5ユニットずれてスペースを作る。 |
| FWD | 相手ゴールの65%地点付近に前線ランを行い、ホームポジションからY方向に±2ユニットの揺れを持つ。 |

#### 6.3.4 相手がボール保持時（守備）

| 役割 | 守備時の行動 |
|------|------------|
| FWD | 保持者との距離が5ユニット以内なら軽いプレス（保持者と自陣ゴールの間の10%地点へ移動）。それ以外はホームポジション方向へ緩やかに後退。 |
| MID | 保持者との距離が4.5ユニット以内ならプレス（15%地点へ移動）。それ以外はホームポジションをボール位置方向にシフト（X方向最大3.5、Y方向最大3.0）。 |
| DEF | ディフェンスラインを維持し、ボール位置方向にシフト（X方向最大2.5、Y方向最大3.0）。積極的なプレスは行わない。 |

#### 6.3.5 デフォルト行動

上記いずれの条件にも該当しない場合（ボールが誰にも保持されていない等）、ホームポジションをボール位置方向に緩やかにシフトした位置へ移動する。

### 6.4 パス先評価の詳細

#### 6.4.1 通常モード (`bestPass`)

```
対象: 同チームの全プレイヤー（自分自身を除く）
距離制限: 1.0 < distance < 18.0
スコア = openness * 2 + goalProgress * 0.5 - distance * 0.12
パスレーンがブロックされている場合: score -= 4
最高スコアのプレイヤーを選択（全員が負のスコアでも選択される）
```

#### 6.4.2 リラックスモード (`bestPass` with `relaxed = true`)

GKや自陣深い位置で使用される。パスレーンのブロックに対するペナルティが軽減される（-4 → -1.5）。これにより、多少ブロックされていてもパスが実行され、ボール保持のデッドロックが回避される。

#### 6.4.3 パスレーンブロック判定 (`laneBlocked`)

パサーからターゲットへの直線上に、敵プレイヤーが1.0ユニット以内に存在するかを判定する。直線の始点から0.5ユニット以内、および終点から0.5ユニット以内の領域は判定から除外される。

### 6.5 パス実行の詳細 (`doPassTo`)

パスターゲットが移動中の場合、移動方向にリードパス（先読み補正）が加わる。リード量は `min(distance * 0.1, 1.2)` ユニットである。さらに、`passAccuracy` に基づくランダム誤差（`(1 - passAccuracy) * 1.5` の範囲）がX・Y座標それぞれに加算される。

---

## 7. ボール物理と状態遷移

### 7.1 ボールの3つの状態

ボールは以下の3つの状態を遷移する。

```
[保持中] ←→ [フリー（飛行中）] ←→ [デッド（停止）]
   ↑              ↓                      ↓
   └──── インターセプト ────┘     └── 自動回収 ──→ [保持中]
```

**保持中** (`owner !== null, free === false`): ボールは保持者の前方0.22ユニットに追従する。保持者の `face` ベクトル方向にオフセットされる。

**フリー** (`owner === null, free === true`): ボールは `vel` に従って移動し、`looseBallDrag`（3.5 units/s²）で減速する。速度が0.1 units/s以下になると停止する。

**デッド**: フリー状態で速度が0.5 units/s以下の状態が `deadBallTime`（0.7秒）以上継続すると、最寄りのプレイヤーに自動的にボールが渡される。

### 7.2 壁反射

ボールがピッチ境界に到達した場合、以下の処理が行われる。

| 境界 | 処理 |
|------|------|
| 上下端（`\|y\| > pitchHalfH`） | Y座標をクランプし、Y速度を反転・減衰（×-0.4） |
| 左右端（`\|x\| > pitchHalfW`） | ゴール判定を先に行い、ゴールでなければX座標をクランプしX速度を反転・減衰（×-0.4） |

### 7.3 ゴール判定

ボールが左右端に到達した際、Y座標が `[-goalHalfH, +goalHalfH]` の範囲内であればゴールと判定される。具体的には:

- `pos.x >= pitchHalfW - 0.05` かつ `|pos.y| <= goalHalfH` → BLUE チームの得点（`sL++`）
- `pos.x <= -pitchHalfW + 0.05` かつ `|pos.y| <= goalHalfH` → RED チームの得点（`sR++`）

### 7.4 所有権変更とクールダウン

ボールの所有権が変更されると、0.35秒のクールダウンが発生する。クールダウン中はインターセプトが無効化される。これにより、隣接する2選手間でボールが瞬時に往復する「ピンポン現象」が防止される。

### 7.5 インターセプト

インターセプトは2種類存在する。

| 種類 | 条件 | 判定半径 |
|------|------|---------|
| フリーボールの拾得 | ボールがフリー状態で、プレイヤーとの距離が `interceptRadius` 以内 | 0.75 units |
| ドリブラーからの奪取 | ボールが保持されており、敵チームのプレイヤーとの距離が `interceptRadius * 0.65` 以内 | 0.4875 units |

いずれもクールダウン中（`cooldown > 0`）は発生しない。

---

## 8. 試合フロー

### 8.1 試合の状態遷移

```
[初期化] → [キックオフ] → [プレイ中] → [ゴール] → [一時停止] → [キックオフ] → ...
                                          ↓
                                    [タイムアップ] → [FULL TIME表示] → [自動リスタート]
```

### 8.2 キックオフ処理 (`doKickOff`)

キックオフ時に以下の処理が実行される。

1. 全プレイヤーをホームポジションにリセット
2. 全プレイヤーのアクション状態を `"idle"` にリセット
3. 全プレイヤーの向きを相手ゴール方向に設定
4. 意思決定タイマーをランダムに初期化（0〜`decisionInterval` の範囲）
5. ボールをピッチ中央 `(0, 0)` に配置
6. キックオフ側チームの中央に最も近いプレイヤーにボールを渡す

### 8.3 ゴール後の処理

1. 得点をインクリメント
2. `paused = true` に設定し、`pauseT = goalResetDelay`（2.0秒）をセット
3. 画面フラッシュ（1.8秒）と `"GOAL!"` テキストを表示
4. 失点したチームの次のキックオフを設定（`koSide = -goalSide`）
5. 一時停止時間が経過したら `doKickOff` を実行

### 8.4 試合終了と自動リスタート

経過時間が `matchDuration`（120秒）に達すると、`"FULL TIME"` のフラッシュ（2.5秒）が表示され、5秒後に新しい試合が自動的に開始される。リスタート時は `mkState()` で完全に新しい状態が生成され、`doKickOff` が呼び出される。

---

## 9. 更新ループ (`update`)

`update` 関数は `requestAnimationFrame` コールバック内で毎フレーム呼び出される。デルタタイムは `Math.min(0.05, elapsed)` でキャップされ、フレーム落ち時の大きなジャンプを防止する。また、`dt < 0.001` または `dt >= 0.1` の異常値はスキップされる。

### 9.1 更新順序

1. **フラッシュ減衰**: `flash > 0` なら `flash -= dt`
2. **試合終了チェック**: `over === true` ならリスタートタイマーを減算
3. **一時停止チェック**: `paused === true` なら一時停止タイマーを減算
4. **経過時間更新**: `time += dt`、`matchDuration` 超過で試合終了
5. **クールダウン減算**: `cooldown -= dt`
6. **デッドボール回復**: 所有者なし・非フリー状態の修正、低速ボールの自動回収
7. **ボール物理**: 保持中は追従、フリーは移動・壁反射・ゴール判定・減速
8. **トレイル減衰**: `trail.t -= dt`、0以下で `null` に
9. **プレイヤーループ**（22人分）:
   - インターセプト判定
   - 意思決定タイマー減算・意思決定実行
   - 移動処理（`vmove` + `pitchClamp`）

---

## 10. 描画仕様 (`render`)

### 10.1 座標変換

ワールド座標からCanvas画素座標への変換は以下の式で行われる。

```
スケール sc = min(canvasWidth / (pitchHalfW * 2 + 2.5), canvasHeight / (pitchHalfH * 2 + 3.5))
原点 ox = canvasWidth / 2
原点 oy = canvasHeight / 2 + 0.75 * sc（HUD分の下方オフセット）

screenX = ox + worldX * sc
screenY = oy - worldY * sc（Y軸反転）
```

### 10.2 描画レイヤー順序

描画は以下の順序で行われる（後に描画されるものが上に表示される）。

| 順序 | レイヤー | 説明 |
|------|---------|------|
| 1 | 背景 | `#0a0a10` の暗色で全画面塗りつぶし |
| 2 | ピッチ | 緑のグラデーション + 12本の縦縞パターン |
| 3 | ピッチライン | 外枠、センターライン、センターサークル、ペナルティエリア、ゴールエリア、ペナルティスポット、ペナルティアーク、コーナーアーク |
| 4 | ゴール | 半透明の白い矩形 + 枠線 + ゴールポスト（白い小円） |
| 5 | 軌跡（Trail） | パス: 白い点線（`setLineDash`）、シュート: オレンジの太線 |
| 6 | プレイヤー | 円形（ラジアルグラデーション）+ 背番号テキスト + 保持者リング |
| 7 | ボール | 影（楕円）+ 白いラジアルグラデーション円 + 黒い枠線 |
| 8 | フラッシュ | 半透明の黄白色オーバーレイ + テキスト（`"GOAL!"` / `"FULL TIME"`） |
| 9 | HUD | スコアバー（角丸矩形）+ チーム名 + スコア + タイマー（台形背景） |

### 10.3 カラーパレット

| 用途 | カラーコード | 説明 |
|------|------------|------|
| 背景 | `#0a0a10` | ダークネイビー |
| ピッチ（明） | `#1a6b3a` | フォレストグリーン |
| ピッチ（暗） | `#145e30` | ダークグリーン |
| ライン | `rgba(255,255,255,0.75)` | 半透明白 |
| BLUE チーム | `#2563eb`（基本）/ `#60a5fa`（ハイライト） | ロイヤルブルー系 |
| RED チーム | `#dc2626`（基本）/ `#f87171`（ハイライト） | クリムゾンレッド系 |
| 保持リング BLUE | `rgba(37,99,235,0.5)` | 半透明ブルー |
| 保持リング RED | `rgba(220,38,38,0.5)` | 半透明レッド |
| HUD背景 | `rgba(10,10,20,0.88)` | 半透明ダーク |
| HUDテキスト | `#fff` | 白 |
| タイマーテキスト | `#aabbcc` | ライトブルーグレー |

### 10.4 プレイヤー描画の詳細

各プレイヤーは以下の要素で構成される。

1. **保持者リング**（ボール保持者のみ）: プレイヤー半径の1.6倍の円。チームカラーの半透明ストローク。加えて `shadowBlur` によるグロー効果。
2. **プレイヤー円**: ラジアルグラデーション（中心がハイライトカラー、外周が基本カラー）。半径は `playerRadius * sc` ピクセル。
3. **背番号**: 白色の太字テキスト。フォントサイズは半径に比例（最小8px）。中央揃え。

### 10.5 HUD描画の詳細

HUDはピッチ上部に配置されるスコアバーとタイマーで構成される。

**スコアバー**: 角丸矩形（`quadraticCurveTo` による）。幅は画面幅の55%または9ユニットの小さい方。左端にBLUEカラーバー、右端にREDカラーバー。中央にスコア（太字）、左にチーム名「BLUE」、右に「RED」。

**タイマー**: スコアバーの直下に配置される台形背景。残り時間を `MM:SS` 形式で表示。フォントは `Roboto Mono`（等幅）。

---

## 11. Reactコンポーネント構造

### 11.1 `Home` コンポーネント

シミュレーション全体を管理する唯一のReactコンポーネントである。

```typescript
export default function Home() {
  const ref = useRef<HTMLCanvasElement>(null);      // Canvas要素への参照
  const stRef = useRef<State>(mkState());            // 試合状態（再レンダリング不要）
  const ltRef = useRef(0);                           // 前フレームのタイムスタンプ
  // ...
}
```

`useRef` を使用して状態を管理することで、Reactの再レンダリングを回避し、Canvas描画のパフォーマンスを最大化している。

### 11.2 ライフサイクル

`useEffect` 内で以下の処理が実行される。

1. **Canvas初期化**: 2Dコンテキストの取得
2. **キックオフ**: 初回の `doKickOff` 呼び出し
3. **リサイズハンドラ**: `window.devicePixelRatio` を考慮したCanvas解像度の設定
4. **アニメーションループ**: `requestAnimationFrame` による `update` → `render` の繰り返し
5. **クリーンアップ**: `cancelAnimationFrame` とリサイズリスナーの解除

### 11.3 Canvas要素

```html
<canvas style={{
  display: "block",
  width: "100vw",
  height: "100vh",
  background: "#0a0a10",
  touchAction: "none"    // モバイルでのスクロール防止
}} />
```

`touchAction: "none"` により、iPhoneでの意図しないスクロールやズームが防止される。

---

## 12. レスポンシブ対応

### 12.1 Canvas解像度

`window.resize` イベントで以下の処理が実行される。

```typescript
const dpr = window.devicePixelRatio || 1;
canvas.width = window.innerWidth * dpr;
canvas.height = window.innerHeight * dpr;
canvas.style.width = window.innerWidth + "px";
canvas.style.height = window.innerHeight + "px";
```

`devicePixelRatio` を考慮することで、Retinaディスプレイ等の高解像度画面でもシャープな描画が維持される。

### 12.2 スケーリング

描画スケールは `min(canvasWidth / pitchTotalW, canvasHeight / pitchTotalH)` で計算されるため、画面のアスペクト比に関わらずピッチ全体が常に表示される。横長画面ではピッチの上下に余白が、縦長画面では左右に余白が生じる。

### 12.3 フォントサイズ

すべてのフォントサイズはスケール値 `sc` に比例して計算され、`Math.max` で最小サイズが保証される。これにより、小さな画面でもテキストの可読性が維持される。

---

## 13. 既知の制約と今後の拡張可能性

### 13.1 現在の制約

| 項目 | 説明 |
|------|------|
| オフサイド | 未実装。FWDが相手DFラインの裏に自由に走り込める。 |
| ファウル・カード | 未実装。タックルやファウルの概念がない。 |
| スローイン・コーナーキック・ゴールキック | 未実装。ボールがピッチ外に出た場合は壁反射で処理される。 |
| GKの手によるセーブ | 未実装。GKは他のプレイヤーと同じインターセプト判定のみ。 |
| 選手交代 | 未実装。22人が試合終了まで固定。 |
| 体力・疲労 | 未実装。全選手が一定速度で動き続ける。 |
| フォーメーション変更 | 実行時の動的変更は未対応。コード上の `FORM_442` を書き換える必要がある。 |

### 13.2 拡張可能性

| 拡張項目 | 実装方針 |
|---------|---------|
| フォーメーション切替 | `FORM_442` を複数定義し、UIまたはパラメータで選択可能にする |
| 選手個別能力値 | `Player` に `speed`, `accuracy`, `stamina` 等を追加し、`P` のグローバル値をオーバーライド |
| 試合統計 | `State` にポゼッション率、シュート数、パス成功率等のカウンターを追加 |
| リプレイ機能 | 各フレームの状態をバッファに記録し、逆再生・スロー再生を実装 |
| 音声効果 | Web Audio API でホイッスル音、キック音、歓声等を追加 |
| 実在チームデータ | 選手名・背番号・チームカラーを外部JSONから読み込み |

---

## 付録A: 関数一覧

| 関数名 | 引数 | 戻り値 | 説明 |
|--------|------|--------|------|
| `slotRole` | `slot: number` | `Role` | スロット番号から役割を返す |
| `mkPlayers` | なし | `Player[]` | 22人のプレイヤーを初期化 |
| `mkState` | なし | `State` | 試合状態を初期化 |
| `checkGoal` | `pos: V` | `number` | ゴール判定（1: 右ゴール、-1: 左ゴール、0: なし） |
| `give` | `ball: Ball, idx: number` | `void` | ボールをプレイヤーに渡す |
| `kick` | `st, dir, spd, shot, tgt` | `void` | ボールを蹴る（パス/シュート共通） |
| `nearest` | `st, pos, teamFilter?` | `number` | 最寄りプレイヤーのインデックスを返す |
| `openness` | `st, p` | `number` | プレイヤーの「開き具合」（最寄り敵との距離） |
| `laneBlocked` | `st, from, to, team` | `boolean` | パスレーンがブロックされているか |
| `bestPass` | `st, idx, relaxed?` | `number \| null` | 最適なパス先を返す |
| `doPassTo` | `st, idx, targetIdx` | `void` | 指定先へパスを実行 |
| `doDribble` | `st, idx` | `void` | ドリブルを実行 |
| `decideHasBall` | `st, idx` | `void` | ボール保持時の意思決定 |
| `decideNoBall` | `st, idx` | `void` | ボール非保持時の意思決定 |
| `doKickOff` | `st` | `void` | キックオフ処理 |
| `update` | `st, dt` | `void` | 1フレーム分の状態更新 |
| `render` | `ctx, canvas, st` | `void` | 1フレーム分の描画 |

---

## 付録B: パラメータ調整ガイド

以下に、特定の効果を得るためのパラメータ調整の指針を示す。

| 目的 | 調整するパラメータ | 方向 |
|------|------------------|------|
| 試合のテンポを速くする | `moveSpeed` ↑, `passSpeed` ↑, `decisionInterval` ↓ | 全体的に速度を上げ、判断を頻繁にする |
| ゴールを増やす | `shotAccuracy` ↑, `shotRange` ↑, `shotAngle` ↑ | シュートの成功率と機会を増やす |
| ゴールを減らす | `shotAccuracy` ↓, `interceptRadius` ↑ | シュート精度を下げ、インターセプトを増やす |
| パスサッカーにする | `passAccuracy` ↑, `dribbleControl` ↓ | パスを正確にし、ドリブルを失敗しやすくする |
| ドリブル中心にする | `dribbleControl` ↑, `dribbleSpeed` ↑ | ドリブルの成功率と速度を上げる |
| よりリアルな試合時間 | `matchDuration` → 5400（90分相当） | 試合時間を延長 |
| ボールの動きを遅くする | `looseBallDrag` ↑ | フリーボールの減速を強くする |

---
