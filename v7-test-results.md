# フットサルシミュレーション v7.1→v7.2 テスト結果報告

## テスト概要

**実施日時**: 2026年2月21日  
**テスト対象**: バックパス精度とオウンゴール防止機能  
**テスト方法**: 10試合の並列実行による統計的検証

---

## v7.1 テスト結果（初回検証）

### 統計データ

| 項目 | 結果 |
|------|------|
| **総試合数** | 10試合 |
| **オウンゴール発生試合** | 2/10試合（20.0%） |
| **オウンゴール総数** | 2ゴール |
| **バックパスエラー発生試合** | 7/10試合（70.0%） |
| **バックパスエラー総数** | 13回 |
| **平均エラー数/試合** | 1.30回 |
| **成功したバックパス総数** | 36回 |
| **平均成功数/試合** | 4.00回 |
| **バックパス成功率** | **73.5%** |

### 安全性評価分布

- **Excellent**: 1/10試合（10.0%）
- **Good**: 2/10試合（20.0%）
- **Fair**: 4/10試合（40.0%）
- **Poor**: 3/10試合（30.0%）

### 問題点の特定

v7.1の実装では以下の致命的な問題が発見されました:

1. **kick関数呼び出し前のエラー追加**: 各パス関数（doPass, doLongPassTo等）でターゲット位置に既にランダムエラーが加算されていた
2. **kick関数内の安全装置が無効化**: 既にズレた座標が渡されるため、kick関数内のオウンゴール防止ロジックが機能しない
3. **バックパス精度向上が適用されない**: エラー削減（90%→10%）が実際には適用されていなかった

**具体例（line 524-526）:**
```typescript
tp.x += rng(-baseErr, baseErr); tp.y += rng(-baseErr, baseErr);  // ← ここで既にエラー追加
kick(st, me.face, P.passSpeed, false, tp);  // ← kick関数に渡される時点で既にズレている
```

---

## v7.2 改善内容

### 根本的な修正

1. **エラー処理の統一化**
   - 全てのkick()呼び出し前のランダムエラー追加を削除
   - kick()関数内で統一的にエラー処理を実施
   - customErrパラメータを追加して、各パス関数から適切なエラー範囲を渡す

2. **バックパス精度の大幅向上**
   - エラー削減率を90%→**95%**に強化
   - `errRange *= 0.05` （v7.1は0.1）

3. **GK特化型オウンゴール防止**
   - 自陣ゴール2.0ユニット以内へのパスを「GKへのパス」と判定
   - Y軸を強制的にゴール枠外（±2.22ユニット）へ移動
   - X軸を0.5ユニット前方へ移動
   - エラー範囲を完全にゼロに設定

### 修正されたコード箇所

- `kick()` 関数: 統一的なエラー処理とGK安全装置
- `doPass()`: エラー追加削除、baseErrをkick()へ渡す
- `doLongPassTo()`: エラー追加削除、errをkick()へ渡す
- `doLongPassToAdvanced()`: エラー追加削除、errをkick()へ渡す

---

## v7.2 初期テスト結果

### 観察結果（60秒間）

- **スコア**: 0-0
- **オウンゴール**: 観察されず
- **バックパスエラー**: 目視では確認されず
- **バックパスの安定性**: 選手の足元に「ピタッ」と収まる様子を確認

### 期待される改善

v7.2の修正により、以下の劇的な改善が期待されます:

| 項目 | v7.1 | v7.2（期待値） |
|------|------|----------------|
| オウンゴール発生率 | 20% | **0-5%** |
| バックパスエラー率 | 26.5% | **5-10%** |
| バックパス成功率 | 73.5% | **90-95%** |
| 安全性評価（Good以上） | 30% | **70-80%** |

---

## 技術的詳細

### v7.2 kick関数の改善ロジック

```typescript
function kick(st: State, dir: V, spd: number, shot: boolean, tgt: V, 
              isLong: boolean = false, customErr?: number) {
  // 1. カスタムエラー範囲の使用
  let errRange = customErr !== undefined ? customErr : 
                 (shot ? (1 - P.shotAccuracy) * 3.0 : 
                  isLong ? (1 - P.longPassAccuracy) * 1.5 : 
                  (1 - P.passAccuracy) * 1.5);
  
  if (!shot && kicker) {
    // 2. バックパス検出と精度向上
    const isForward = (tgt.x - kicker.pos.x) * -kicker.team > 0.5;
    if (!isForward) {
      errRange *= 0.05; // 95%エラー削減
    }
    
    // 3. GK特化型安全装置
    const distToOwnGoal = Math.sqrt(Math.pow(tgt.x - ownGoalX, 2) + 
                                    Math.pow(tgt.y - ownGoalY, 2));
    const targetIsGK = distToOwnGoal < 2.0;
    
    if (targetIsGK) {
      errRange = 0; // 完全にゼロ
      // Y軸を強制的にゴール枠外へ
      const safeOffsetY = P.goalHalfH + 1.0; // 2.22ユニット
      if (Math.abs(finalTarget.y) < safeOffsetY) {
        finalTarget.y = (finalTarget.y >= 0 ? 1 : -1) * safeOffsetY;
      }
      // X軸を前方へ
      const safeOffsetX = 0.5;
      if (Math.abs(finalTarget.x - ownGoalX) < safeOffsetX) {
        finalTarget.x = ownGoalX - kicker.team * safeOffsetX;
      }
    }
  }
}
```

---

## 結論

v7.1の並列テストにより、実装が不十分であることが統計的に証明されました（オウンゴール20%、エラー率26.5%）。v7.2では根本的な原因（kick関数呼び出し前のエラー追加）を特定し、エラー処理を統一化することで、バックパス成功率を**73.5%→90-95%**へ向上させることが期待されます。

### 次のステップ

1. v7.2の10試合並列テストを実施して統計的改善を検証
2. チェックポイント作成とGitHubへのプッシュ
3. さらなる戦術的改善（プレス強度調整、統計表示等）の実装

---

**作成日時**: 2026年2月21日 08:00 JST  
**バージョン**: v7.2  
**テスト実施者**: Manus AI Agent
